<header></header>

<style>
  header {
    position: relative;
    display: flex;
    align-items: last baseline;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
    max-width: var(--max-w);
    margin-inline: auto;
    /* padding: 0.4rem 1rem; */
  }
</style>
